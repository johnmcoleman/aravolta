# Copy to .env for local development: `cp .env.example .env`
# These match the local infra in docker-compose.yml. .env is gitignored;
# never commit real credentials.
DATABASE_URL=postgresql://postgres:dev@localhost:5432/telemetry
CACHE_URL=redis://localhost:6379/0
